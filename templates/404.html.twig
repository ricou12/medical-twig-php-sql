<!DOCTYPE html>
<html lang="fr">

<head>
    {% include 'includes/head.html.twig'%}
    <title>error</title>
</head>

<body>
    <main>
        <div class="erreur "></div>
        <div class="container-fluid text-white">
            <div class="row rounded p-5 mt-1">
                <div class="col-12 text-center">
                    <h1 class="text-info">PAGE NOT FOUND</h1>
                    <h3 class="mt-4">{{ message }}</h3>
                </div>
                <div class="col-12 text-center mt-4 ">
                    <form action="index.php?routing=home" method="POST">
                        <button type="submit" class="btn btn-outline-light"><h3>Retour en page d'accueil</h3></button>
                    </form>
                 
                </div>
            </div>
        </div>
    </main>
    {% include 'includes/script.html.twig' %}
</body>

</html>





{# {% extends "default.html.twig" %}

{% block title %}
    Error
{% endblock %}
{% block content %}
    <div class="container-fluid">
       <div class="row">
            <div class="error"></div>
            <div class="col-12 d-flex justify-content-center">
            </div>
            <div class="col-12">
               <p>{{ message }}</p>
            </div>
       </div> 
    </div>
{% endblock %} #}